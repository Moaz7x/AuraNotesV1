# Sprint 1
## Epic 1: Core Engine & Infrastructure
### Story: Project Initialization & CI/CD
1- Task: Setup Gradle Version Catalogs and Project Structure. (AGENT : Tech Lead)
1- Subtask: Create `gradle/libs.versions.toml` file. (AGENT : Software Engineer)
2- Subtask: Define `[versions]` block with `kotlin = "2.4.10"`, `agp = "8.1.0"`, `ink = "1.0.0"`, `room = "2.6.1"`. (AGENT : Software Engineer)
3- Subtask: Define `[libraries]` block for `androidx-ink-nativeloader`, `androidx-ink-authoring`, `androidx-ink-rendering`, `androidx-ink-geometry`. (AGENT : Software Engineer)
4- Subtask: Define `[libraries]` block for `kotlinx-serialization-protobuf` and `androidx-room-runtime`. (AGENT : Software Engineer)
5- Subtask: Configure `build.gradle.kts` (Project level) to apply Kotlin and Android application plugins. (AGENT : Software Engineer)
6- Subtask: Configure `build.gradle.kts` (App level) to set `minSdk = 24` and `targetSdk = 34`. (AGENT : Software Engineer)
2- Task: Configure Git and CI/CD Pipelines. (AGENT : DevOps Engineer)
1- Subtask: Initialize Git repository and create `.gitignore` for Android Studio. (AGENT : DevOps Engineer)
2- Subtask: Create `.github/workflows/ci.yml` file. (AGENT : DevOps Engineer)
3- Subtask: Write YAML step to checkout code `uses: actions/checkout@v3`. (AGENT : DevOps Engineer)
4- Subtask: Write YAML step to setup JDK 20 `uses: actions/setup-java@v3`. (AGENT : DevOps Engineer)
5- Subtask: Write YAML step to run `./gradlew ktlintCheck`. (AGENT : DevOps Engineer)
6- Subtask: Write YAML step to run `./gradlew testDebugUnitTest`. (AGENT : DevOps Engineer)

### Story: UDF State Management Contracts
1- Task: Create `CanvasUiState.kt` data class. (AGENT : Software Engineer)
1- Subtask: Define `val isLoading: Boolean = true` variable. (AGENT : Software Engineer)
2- Subtask: Define `val activeTool: ToolType = ToolType.PEN` variable. (AGENT : Software Engineer)
3- Subtask: Define `val visibleStrokes: List<Stroke> = emptyList()` variable. (AGENT : Software Engineer)
4- Subtask: Define `val currentAudioTimeMs: Long = 0L` variable. (AGENT : Software Engineer)
2- Task: Create `CanvasIntent.kt` sealed class. (AGENT : Software Engineer)
1- Subtask: Define `data class OnStrokeDrawn(val stroke: Stroke) : CanvasIntent()` object. (AGENT : Software Engineer)
2- Subtask: Define `data class OnViewportChanged(val bounds: RectF) : CanvasIntent()` object. (AGENT : Software Engineer)
3- Subtask: Define `data class ChangeTool(val tool: ToolType) : CanvasIntent()` object. (AGENT : Software Engineer)
3- Task: Create `CanvasViewModel.kt` class. (AGENT : Software Engineer)
1- Subtask: Initialize `private val _uiState = MutableStateFlow(CanvasUiState())` variable. (AGENT : Software Engineer)
2- Subtask: Initialize `val uiState: StateFlow<CanvasUiState> = _uiState.asStateFlow()` variable. (AGENT : Software Engineer)
3- Subtask: Create `fun processIntent(intent: CanvasIntent)` method. (AGENT : Software Engineer)
4- Subtask: Implement `when(intent)` switch statement inside `processIntent` method. (AGENT : Software Engineer)

### Story: Jetpack Ink SurfaceView Implementation
1- Task: Create `AuraCanvasView.kt` class extending `SurfaceView`. (AGENT : Software Engineer)
1- Subtask: Initialize `private val inkAuthoring = InkAuthoring()` variable. (AGENT : Software Engineer)
2- Subtask: Initialize `private val strokeRenderer = CanvasStrokeRenderer()` variable. (AGENT : Software Engineer)
3- Subtask: Initialize `private val currentBrush = Brush.createStockBrush(StockBrushes.Pen)` variable. (AGENT : Software Engineer)
4- Subtask: Override `fun surfaceCreated(holder: SurfaceHolder)` method. (AGENT : Software Engineer)
5- Subtask: Override `fun surfaceDestroyed(holder: SurfaceHolder)` method. (AGENT : Software Engineer)
2- Task: Implement Touch Event Ingestion in `AuraCanvasView.kt`. (AGENT : Software Engineer)
1- Subtask: Override `fun onTouchEvent(event: MotionEvent): Boolean` method. (AGENT : Software Engineer)
2- Subtask: Implement `MotionEvent.ACTION_DOWN` case to call `inkAuthoring.startStroke(event)`. (AGENT : Software Engineer)
3- Subtask: Implement `MotionEvent.ACTION_MOVE` case to call `inkAuthoring.addToStroke(event)`. (AGENT : Software Engineer)
4- Subtask: Implement `MotionEvent.ACTION_UP` case to call `inkAuthoring.finishStroke(event)`. (AGENT : Software Engineer)
5- Subtask: Create `private fun renderCurrentStroke(canvas: Canvas)` method. (AGENT : Software Engineer)
6- Subtask: Call `strokeRenderer.drawStroke(canvas, inkAuthoring.getInProgressStroke(), currentBrush)` inside `renderCurrentStroke`. (AGENT : Software Engineer)
3- Task: Write SurfaceView Unit Tests. (AGENT : QA / Automation Engineer)
1- Subtask: Create `AuraCanvasViewTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testActionDownInitializesStroke()` method using MockK to verify `inkAuthoring.startStroke()` is called. (AGENT : QA / Automation Engineer)

# Sprint 2
## Epic 2: Local Data Layer & Protobuf
### Story: Room Database Metadata Index
1- Task: Create `NoteEntity.kt` data class. (AGENT : Software Engineer)
1- Subtask: Add `@PrimaryKey val id: String` variable. (AGENT : Software Engineer)
2- Subtask: Add `val title: String` variable. (AGENT : Software Engineer)
3- Subtask: Add `val folderId: String?` variable. (AGENT : Software Engineer)
4- Subtask: Add `val canvasType: String` variable. (AGENT : Software Engineer)
2- Task: Create `NoteDao.kt` interface. (AGENT : Software Engineer)
1- Subtask: Define `@Query("SELECT * FROM notes") fun observeAllNotes(): Flow<List<NoteEntity>>` method. (AGENT : Software Engineer)
2- Subtask: Define `@Insert(onConflict = OnConflictStrategy.REPLACE) suspend fun insertNote(note: NoteEntity)` method. (AGENT : Software Engineer)
3- Subtask: Define `@Query("DELETE FROM notes WHERE id = :noteId") suspend fun deleteNote(noteId: String)` method. (AGENT : Software Engineer)
3- Task: Create `AppDatabase.kt` abstract class. (AGENT : Software Engineer)
1- Subtask: Add `@Database(entities = [NoteEntity::class], version = 1)` annotation. (AGENT : Software Engineer)
2- Subtask: Define `abstract fun noteDao(): NoteDao` method. (AGENT : Software Engineer)

### Story: Protobuf Serialization & ZipOutputStream
1- Task: Create Protobuf DTO classes. (AGENT : Software Engineer)
1- Subtask: Create `@Serializable data class PointDto` with `val x: Float`, `val y: Float`, `val pressure: Float` variables. (AGENT : Software Engineer)
2- Subtask: Create `@Serializable data class StrokeDto` with `val id: String`, `val toolType: Int`, `val points: List<PointDto>` variables. (AGENT : Software Engineer)
3- Subtask: Create `@Serializable data class StrokeChunkDto` with `val chunkId: String`, `val strokes: List<StrokeDto>` variables. (AGENT : Software Engineer)
2- Task: Create `FileManager.kt` class for `.auranote` I/O. (AGENT : Software Engineer)
1- Subtask: Create `suspend fun writeZipArchive(noteId: String, payload: ByteArray)` method. (AGENT : Software Engineer)
2- Subtask: Initialize `val tempFile = File(context.filesDir, "$noteId.tmp")` variable inside `writeZipArchive`. (AGENT : Software Engineer)
3- Subtask: Implement `ZipOutputStream(FileOutputStream(tempFile))` logic to write `payload` to `strokes.bin` entry. (AGENT : Software Engineer)
4- Subtask: Implement `tempFile.renameTo(File(context.getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS), "$noteId.auranote"))` method call for atomic write. (AGENT : Software Engineer)
5- Subtask: Create `suspend fun readZipArchive(noteId: String): ByteArray` method. (AGENT : Software Engineer)
6- Subtask: Implement `ZipInputStream` logic to extract `strokes.bin` entry into a `ByteArray`. (AGENT : Software Engineer)
3- Task: Create `SaveStrokeChunkUseCase.kt` class. (AGENT : Software Engineer)
1- Subtask: Create `suspend operator fun invoke(chunk: StrokeChunkDto)` method. (AGENT : Software Engineer)
2- Subtask: Call `ProtoBuf.encodeToByteArray(StrokeChunkDto.serializer(), chunk)` method. (AGENT : Software Engineer)
3- Subtask: Call `fileManager.writeZipArchive(noteId, byteArray)` method. (AGENT : Software Engineer)

# Sprint 3
## Epic 3: Adaptive UI Shell & Toolbars
### Story: Dashboard Layout (Material 3)
1- Task: Create `Theme.kt` and `Color.kt`. (AGENT : Software Engineer)
1- Subtask: Define `val AuraIndigo = Color(0xFF4F46E5)` variable in `Color.kt`. (AGENT : Software Engineer)
2- Subtask: Define `val DarkSurface = Color(0xFF1E293B)` variable in `Color.kt`. (AGENT : Software Engineer)
3- Subtask: Create `@Composable fun AuraNotesTheme(darkTheme: Boolean, content: @Composable () -> Unit)` method in `Theme.kt`. (AGENT : Software Engineer)
2- Task: Create `DashboardScreen.kt` Composable. (AGENT : Software Engineer)
1- Subtask: Initialize `val notes by viewModel.notes.collectAsStateWithLifecycle()` variable. (AGENT : Software Engineer)
2- Subtask: Implement `Scaffold` with `floatingActionButton = { AddNoteFab() }` parameter. (AGENT : Software Engineer)
3- Subtask: Create `@Composable fun SearchBarPill()` method with `TextField` and `RoundedCornerShape(50)`. (AGENT : Software Engineer)
4- Subtask: Create `@Composable fun NoteGrid(notes: List<NoteEntity>)` method using `LazyVerticalGrid`. (AGENT : Software Engineer)
3- Task: Create `NoteCard.kt` Composable. (AGENT : Software Engineer)
1- Subtask: Implement `Card` with `elevation = CardDefaults.cardElevation(defaultElevation = 1.dp)`. (AGENT : Software Engineer)
2- Subtask: Add `Text(text = note.title, style = MaterialTheme.typography.titleMedium)` method call. (AGENT : Software Engineer)

### Story: Dual Floating Toolbars & Edge Snapping
1- Task: Create `ToolbarState.kt` data class. (AGENT : Software Engineer)
1- Subtask: Define `val offsetX: Animatable<Float, AnimationVector1D>` variable. (AGENT : Software Engineer)
2- Subtask: Define `val offsetY: Animatable<Float, AnimationVector1D>` variable. (AGENT : Software Engineer)
3- Subtask: Define `val orientation: Orientation = Orientation.VERTICAL` variable. (AGENT : Software Engineer)
2- Task: Create `FloatingToolbar.kt` Composable. (AGENT : Software Engineer)
1- Subtask: Implement `Surface` with `shape = RoundedCornerShape(24.dp)`. (AGENT : Software Engineer)
2- Subtask: Add `Modifier.pointerInput(Unit)` to the `Surface`. (AGENT : Software Engineer)
3- Subtask: Call `detectDragGestures` inside `pointerInput`. (AGENT : Software Engineer)
4- Subtask: Implement `onDrag` lambda to update `offsetX.snapTo()` and `offsetY.snapTo()` methods. (AGENT : Software Engineer)
3- Task: Implement Edge Snapping Math in `FloatingToolbar.kt`. (AGENT : Software Engineer)
1- Subtask: Create `fun calculateNearestEdge(x: Float, y: Float, maxX: Float, maxY: Float): Edge` method. (AGENT : Software Engineer)
2- Subtask: Implement `onDragEnd` lambda inside `detectDragGestures`. (AGENT : Software Engineer)
3- Subtask: Call `calculateNearestEdge` inside `onDragEnd`. (AGENT : Software Engineer)
4- Subtask: Call `launch { offsetX.animateTo(targetX) }` method based on the calculated edge. (AGENT : Software Engineer)
5- Subtask: Call `launch { offsetY.animateTo(targetY) }` method based on the calculated edge. (AGENT : Software Engineer)
4- Task: Write Toolbar UI Tests. (AGENT : QA / Automation Engineer)
1- Subtask: Create `FloatingToolbarTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testToolbarSnapsToLeftEdgeOnRelease()` method using `composeTestRule.onNodeWithTag("toolbar").performTouchInput { swipeRight() }`. (AGENT : QA / Automation Engineer)
# Sprint 4
## Epic 4: Audio-Synced Strokes
### Story: Audio Recording & Timestamping
1- Task: Create `AudioRepository.kt` interface and implementation. (AGENT : Software Engineer)
1- Subtask: Define `fun startRecording(noteId: String): Flow<Long>` method in the interface. (AGENT : Software Engineer)
2- Subtask: Define `suspend fun stopRecording(): Result<String>` method in the interface. (AGENT : Software Engineer)
3- Subtask: Initialize `private var mediaRecorder: MediaRecorder? = null` variable in the implementation class. (AGENT : Software Engineer)
4- Subtask: Initialize `private var startTimeMs: Long = 0L` variable in the implementation class. (AGENT : Software Engineer)
5- Subtask: Implement `startRecording` method to configure `MediaRecorder` with `AudioSource.MIC` and `OutputFormat.MPEG_4`. (AGENT : Software Engineer)
6- Subtask: Implement `SystemClock.elapsedRealtime()` call inside `startRecording` to set `startTimeMs`. (AGENT : Software Engineer)
7- Subtask: Create a `flow { while(isRecording) { emit(SystemClock.elapsedRealtime() - startTimeMs); delay(100) } }` to emit playback progress. (AGENT : Software Engineer)
2- Task: Map timestamps to Jetpack Ink Strokes. (AGENT : Software Engineer)
1- Subtask: Update `CanvasIntent.OnStrokeDrawn` to include `val timestampMs: Long?` parameter. (AGENT : Software Engineer)
2- Subtask: Modify `onTouchEvent` in `AuraCanvasView.kt` to capture the current emitted audio time on `MotionEvent.ACTION_UP`. (AGENT : Software Engineer)
3- Subtask: Update `CanvasViewModel.processIntent` to attach the `timestampMs` to the newly created `StrokeDto` before saving to state. (AGENT : Software Engineer)
3- Task: Write Audio Sync Unit Tests. (AGENT : QA / Automation Engineer)
1- Subtask: Create `AudioRepositoryTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testStartRecordingEmitsTimestamps()` method using `app.cash.turbine.test` to verify the Flow emits increasing Long values. (AGENT : QA / Automation Engineer)

### Story: Audio Playback & Ghosting UI
1- Task: Create `AudioPlaybackEngine.kt` class. (AGENT : Software Engineer)
1- Subtask: Initialize `private var mediaPlayer: MediaPlayer? = null` variable. (AGENT : Software Engineer)
2- Subtask: Create `fun playAudio(uri: String): Flow<Long>` method. (AGENT : Software Engineer)
3- Subtask: Implement `mediaPlayer?.setDataSource(uri)` and `mediaPlayer?.prepare()` inside `playAudio`. (AGENT : Software Engineer)
4- Subtask: Create a `flow` that emits `mediaPlayer?.currentPosition?.toLong()` every 16ms (60fps) for smooth UI syncing. (AGENT : Software Engineer)
2- Task: Implement Stroke Ghosting Logic in `CanvasViewModel.kt`. (AGENT : Software Engineer)
1- Subtask: Create `private fun updateVisibleStrokesForPlayback(currentPlaybackTimeMs: Long)` method. (AGENT : Software Engineer)
2- Subtask: Implement `val filteredStrokes = allStrokes.filter { it.audioTimestampMs == null || it.audioTimestampMs <= currentPlaybackTimeMs }` logic. (AGENT : Software Engineer)
3- Subtask: Call `_uiState.update { it.copy(visibleStrokes = filteredStrokes, currentAudioTimeMs = currentPlaybackTimeMs) }` method. (AGENT : Software Engineer)
3- Task: Build Floating Audio Pill UI. (AGENT : Software Engineer)
1- Subtask: Create `@Composable fun AudioPill(audioState: AudioState, timeMs: Long)` method. (AGENT : Software Engineer)
2- Subtask: Implement `Surface` with `RoundedCornerShape(50)` and a red pulsing `Box` for recording state. (AGENT : Software Engineer)
3- Subtask: Create `fun formatTime(ms: Long): String` method to display "MM:SS" inside the pill. (AGENT : Software Engineer)

# Sprint 5
## Epic 5: Hybrid Handwriting Beautification
### Story: ML Kit Integration
1- Task: Setup `DigitalInkRecognition` client. (AGENT : Software Engineer)
1- Subtask: Create `MLKitManager.kt` class. (AGENT : Software Engineer)
2- Subtask: Initialize `private val modelManager = RemoteModelManager.getInstance()` variable. (AGENT : Software Engineer)
3- Subtask: Create `suspend fun downloadModel(languageCode: String): Result<Unit>` method. (AGENT : Software Engineer)
4- Subtask: Implement `DigitalInkRecognitionModelIdentifier.fromLanguageTag(languageCode)` inside `downloadModel`. (AGENT : Software Engineer)
5- Subtask: Create `suspend fun recognizeInk(ink: Ink): Result<String>` method. (AGENT : Software Engineer)
6- Subtask: Implement `DigitalInkRecognizer.getClient(options).recognize(ink).await()` inside `recognizeInk`. (AGENT : Software Engineer)
2- Task: Create `ApplyBeautificationUseCase.kt`. (AGENT : Software Engineer)
1- Subtask: Create `suspend operator fun invoke(strokes: List<StrokeDto>): Result<String>` method. (AGENT : Software Engineer)
2- Subtask: Create `private fun convertToMLKitInk(strokes: List<StrokeDto>): Ink` method. (AGENT : Software Engineer)
3- Subtask: Implement nested loops to map `PointDto(x, y, pressure)` to `Ink.Point.create(x, y, timestamp)` and build `Ink.Stroke`. (AGENT : Software Engineer)
4- Subtask: Call `mlKitManager.recognizeInk(ink)` and return the recognized `String`. (AGENT : Software Engineer)

### Story: UI Replacement & Typography
1- Task: Render recognized text on Canvas. (AGENT : Software Engineer)
1- Subtask: Create `@Serializable data class TextBlockDto(val id: String, val text: String, val x: Float, val y: Float, val fontSize: Float)` data class. (AGENT : Software Engineer)
2- Subtask: Update `CanvasUiState.kt` to include `val textBlocks: List<TextBlockDto> = emptyList()` variable. (AGENT : Software Engineer)
3- Subtask: Update `CanvasViewModel.processIntent` to handle `CanvasIntent.ApplyBeautification`. (AGENT : Software Engineer)
4- Subtask: Implement logic to remove the original `StrokeDto` items from state and append the new `TextBlockDto` to state upon successful ML Kit recognition. (AGENT : Software Engineer)
5- Subtask: Update `AuraCanvasView.kt` `renderCurrentStroke` method to also loop through `textBlocks` and call `canvas.drawText(textBlock.text, textBlock.x, textBlock.y, textPaint)`. (AGENT : Software Engineer)
2- Task: Test ML Kit Memory Consumption. (AGENT : QA / Automation Engineer)
1- Subtask: Create `MLKitMemoryTest.kt` using Jetpack Macrobenchmark. (AGENT : QA / Automation Engineer)
2- Subtask: Write test to repeatedly call `ApplyBeautificationUseCase` 50 times and assert heap allocation remains under 150MB. (AGENT : QA / Automation Engineer)

# Sprint 6
## Epic 6: Infinite Canvas Memory Management
### Story: Spatial Hashing (QuadTree) Implementation
1- Task: Create `QuadTree.kt` class for viewport culling. (AGENT : Software Engineer)
1- Subtask: Define `class QuadTree(val boundary: RectF, val capacity: Int = 10)` class signature. (AGENT : Software Engineer)
2- Subtask: Initialize `private val strokes = mutableListOf<StrokeDto>()` variable. (AGENT : Software Engineer)
3- Subtask: Initialize `private var divided = false` variable. (AGENT : Software Engineer)
4- Subtask: Initialize `private var northWest: QuadTree? = null`, `northEast`, `southWest`, `southEast` variables. (AGENT : Software Engineer)
5- Subtask: Create `fun insert(stroke: StrokeDto, boundingBox: RectF): Boolean` method. (AGENT : Software Engineer)
6- Subtask: Create `private fun subdivide()` method to split the current `boundary` into 4 equal `RectF` quadrants. (AGENT : Software Engineer)
7- Subtask: Create `fun query(range: RectF, found: MutableList<StrokeDto>): List<StrokeDto>` method to return only strokes intersecting the screen viewport. (AGENT : Software Engineer)
2- Task: Integrate QuadTree with ViewModel. (AGENT : Software Engineer)
1- Subtask: Create `CalculateVisibleChunksUseCase.kt` class. (AGENT : Software Engineer)
2- Subtask: Create `operator fun invoke(viewportBounds: RectF, quadTree: QuadTree): List<StrokeDto>` method. (AGENT : Software Engineer)
3- Subtask: Update `CanvasViewModel` to call this UseCase whenever `CanvasIntent.OnViewportChanged` is received. (AGENT : Software Engineer)
3- Task: Write QuadTree Unit Tests. (AGENT : QA / Automation Engineer)
1- Subtask: Create `QuadTreeTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testQueryReturnsOnlyIntersectingStrokes()` method by inserting 1000 strokes and querying a small 100x100 RectF. (AGENT : QA / Automation Engineer)

### Story: Chunk Serialization & Eviction (LRU Cache)
1- Task: Implement LRU Cache for Stroke Chunks. (AGENT : Software Engineer)
1- Subtask: Create `ChunkCacheManager.kt` class. (AGENT : Software Engineer)
2- Subtask: Initialize `private val cache = LruCache<String, StrokeChunkDto>(MAX_CACHE_SIZE_BYTES)` variable. (AGENT : Software Engineer)
3- Subtask: Override `entryRemoved` in `LruCache` to trigger `SaveStrokeChunkUseCase` when a chunk is evicted from memory. (AGENT : Software Engineer)
4- Subtask: Create `suspend fun getChunk(chunkId: String): StrokeChunkDto` method that checks the cache first, then falls back to `FileManager.readZipArchive()`. (AGENT : Software Engineer)
# Sprint 7
## Epic 7: Advanced Tools (Eraser & Lasso)
### Story: Smart Eraser Filtering
1- Task: Implement Eraser Intersection Math. (AGENT : Software Engineer)
1- Subtask: Create `EraserProcessor.kt` class. (AGENT : Software Engineer)
2- Subtask: Create `fun eraseStrokes(eraserPath: Path, strokes: List<StrokeDto>, filter: EraserFilter): List<StrokeDto>` method. (AGENT : Software Engineer)
3- Subtask: Implement `Path.op(strokePath, eraserPath, Path.Op.INTERSECT)` logic inside `eraseStrokes` to detect collisions. (AGENT : Software Engineer)
4- Subtask: Create `enum class EraserFilter { ALL, HIGHLIGHTER_ONLY, TAPE_ONLY }` enum. (AGENT : Software Engineer)
2- Task: Implement "Erase Highlighter Only" Logic. (AGENT : Software Engineer)
1- Subtask: Add `if (filter == EraserFilter.HIGHLIGHTER_ONLY && stroke.toolType != ToolType.HIGHLIGHTER) continue` check inside the `eraseStrokes` loop. (AGENT : Software Engineer)
2- Subtask: Update `CanvasViewModel.processIntent` to handle `CanvasIntent.Erase(path, filter)`. (AGENT : Software Engineer)
3- Subtask: Call `_uiState.update { it.copy(visibleStrokes = remainingStrokes) }` after processing the erasure. (AGENT : Software Engineer)
3- Task: Write Eraser Unit Tests. (AGENT : QA / Automation Engineer)
1- Subtask: Create `EraserProcessorTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testHighlighterFilterIgnoresPenStrokes()` method to verify Pen strokes remain untouched when the filter is applied. (AGENT : QA / Automation Engineer)

### Story: The "X-Ray" Lasso Tool
1- Task: Implement Lasso Selection Math (Point in Polygon). (AGENT : Software Engineer)
1- Subtask: Create `LassoProcessor.kt` class. (AGENT : Software Engineer)
2- Subtask: Create `fun getSelectedStrokes(lassoPolygon: List<PointF>, strokes: List<StrokeDto>): List<StrokeDto>` method. (AGENT : Software Engineer)
3- Subtask: Implement Ray-Casting algorithm inside `getSelectedStrokes` to check if stroke bounding box centers fall inside the `lassoPolygon`. (AGENT : Software Engineer)
2- Task: Build Lasso Context Menu UI. (AGENT : Software Engineer)
1- Subtask: Create `@Composable fun LassoContextMenu(selectedStrokes: List<StrokeDto>, onAction: (LassoAction) -> Unit)` method. (AGENT : Software Engineer)
2- Subtask: Implement `Popup` Composable that anchors to the calculated `RectF` bounds of the selected strokes. (AGENT : Software Engineer)
3- Subtask: Add `IconButton` for "Extract Text", "Change Color", and "Delete". (AGENT : Software Engineer)
3- Task: Implement Lasso Translation (Moving Strokes). (AGENT : Software Engineer)
1- Subtask: Update `CanvasIntent` with `data class TranslateSelectedStrokes(val deltaX: Float, val deltaY: Float)`. (AGENT : Software Engineer)
2- Subtask: Implement logic in `CanvasViewModel` to map over `selectedStrokes` and add `deltaX`/`deltaY` to all `PointDto` coordinates. (AGENT : Software Engineer)

# Sprint 8
## Epic 8: PDF & Media Integration
### Story: PDF Import & Memory-Safe Rendering
1- Task: Setup Android `PdfRenderer`. (AGENT : Software Engineer)
1- Subtask: Create `PdfEngine.kt` class. (AGENT : Software Engineer)
2- Subtask: Initialize `private var pdfRenderer: PdfRenderer? = null` variable. (AGENT : Software Engineer)
3- Subtask: Create `suspend fun openPdf(fileDescriptor: ParcelFileDescriptor)` method. (AGENT : Software Engineer)
4- Subtask: Implement `pdfRenderer = PdfRenderer(fileDescriptor)` inside `openPdf`. (AGENT : Software Engineer)
2- Task: Implement Bitmap Pooling (`inBitmap`) for 2GB RAM safety. (AGENT : Software Engineer)
1- Subtask: Create `BitmapPoolManager.kt` class. (AGENT : Software Engineer)
2- Subtask: Initialize `private val bitmapPool = ArrayDeque<Bitmap>(3)` variable (keeping max 3 pages in memory). (AGENT : Software Engineer)
3- Subtask: Create `fun getReusableBitmap(width: Int, height: Int): Bitmap` method. (AGENT : Software Engineer)
4- Subtask: Create `suspend fun renderPage(pageIndex: Int): Bitmap` method in `PdfEngine.kt`. (AGENT : Software Engineer)
5- Subtask: Implement `val page = pdfRenderer.openPage(pageIndex)` and `page.render(bitmap, null, null, PdfRenderer.Page.RENDER_MODE_FOR_DISPLAY)` inside `renderPage`. (AGENT : Software Engineer)
3- Task: Write PDF Memory Tests. (AGENT : QA / Automation Engineer)
1- Subtask: Create `PdfEngineMemoryTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testPdfRendererDoesNotExceedHeap()` method by rapidly requesting 50 different pages and asserting `bitmapPool` size never exceeds 3. (AGENT : QA / Automation Engineer)

### Story: Image Background Removal (ML Kit)
1- Task: Setup `SubjectSegmentation` client. (AGENT : Software Engineer)
1- Subtask: Update `MLKitManager.kt` to include Subject Segmentation. (AGENT : Software Engineer)
2- Subtask: Initialize `private val segmenter = SubjectSegmentation.getClient(SubjectSegmenterOptions.Builder().enableForegroundBitmap().build())` variable. (AGENT : Software Engineer)
3- Subtask: Create `suspend fun removeBackground(image: Bitmap): Result<Bitmap>` method. (AGENT : Software Engineer)
4- Subtask: Implement `val inputImage = InputImage.fromBitmap(image, 0)` inside `removeBackground`. (AGENT : Software Engineer)
5- Subtask: Implement `val result = segmenter.process(inputImage).await()` and return `result.foregroundBitmap`. (AGENT : Software Engineer)
2- Task: Integrate Background Removal into UI. (AGENT : Software Engineer)
1- Subtask: Create `@Composable fun ImageContextMenu(imageDto: ImageDto)` method. (AGENT : Software Engineer)
2- Subtask: Add "Remove Background" `Button` to the context menu. (AGENT : Software Engineer)
3- Subtask: Update `CanvasViewModel` to handle `CanvasIntent.RemoveImageBackground(imageId)`. (AGENT : Software Engineer)

# Sprint 9
## Epic 9: Page Management & UI Polish
### Story: The "Peek" Sidebar
1- Task: Build Sidebar UI Shell. (AGENT : Software Engineer)
1- Subtask: Create `@Composable fun PeekSidebar(isOpen: Boolean, onDismiss: () -> Unit)` method. (AGENT : Software Engineer)
2- Subtask: Implement `AnimatedVisibility` with `slideInHorizontally` and `slideOutHorizontally` transitions. (AGENT : Software Engineer)
3- Subtask: Implement `Surface` with `Modifier.fillMaxHeight().fillMaxWidth(0.3f)` and a glass-morphism background (using `Modifier.background(Color.Black.copy(alpha = 0.4f))`). (AGENT : Software Engineer)
2- Task: Implement Thumbnail Generation. (AGENT : Software Engineer)
1- Subtask: Create `ThumbnailGenerator.kt` class. (AGENT : Software Engineer)
2- Subtask: Create `suspend fun generateThumbnail(strokes: List<StrokeDto>, width: Int, height: Int): Bitmap` method. (AGENT : Software Engineer)
3- Subtask: Implement logic to draw strokes onto a scaled-down `Canvas` object on `Dispatchers.Default`. (AGENT : Software Engineer)
4- Subtask: Create `@Composable fun ThumbnailGrid(thumbnails: List<Bitmap>)` method inside `PeekSidebar`. (AGENT : Software Engineer)

### Story: Settings & Paper Templates
1- Task: Implement Canvas Background Templates. (AGENT : Software Engineer)
1- Subtask: Create `enum class PaperTemplate { BLANK, DOT_GRID, RULED, GRAPH }` enum. (AGENT : Software Engineer)
2- Subtask: Create `fun drawTemplate(canvas: Canvas, template: PaperTemplate, color: Int)` method in `AuraCanvasView.kt`. (AGENT : Software Engineer)
3- Subtask: Implement `canvas.drawLine()` loops for `RULED` and `GRAPH` templates based on current zoom scale. (AGENT : Software Engineer)
2- Task: Build "More" Radial Menu. (AGENT : Software Engineer)
1- Subtask: Create `@Composable fun RadialMenu(expanded: Boolean, tools: List<ToolType>)` method. (AGENT : Software Engineer)
2- Subtask: Implement `Canvas` composable to draw circular segments using `drawArc`. (AGENT : Software Engineer)
3- Subtask: Implement `pointerInput` to detect angle of drag and select the corresponding tool segment. (AGENT : Software Engineer)

# Sprint 10
## Epic 10: QA, Profiling & Launch Prep
### Story: Macrobenchmark & Profiling
1- Task: Setup Baseline Profiles. (AGENT : Tech Lead)
1- Subtask: Create `benchmark` module in Android Studio. (AGENT : Software Engineer)
2- Subtask: Create `BaselineProfileGenerator.kt` class. (AGENT : Software Engineer)
3- Subtask: Write `baselineProfileRule.collect()` block simulating opening the app, navigating to a note, and drawing a stroke. (AGENT : Software Engineer)
2- Task: Execute Memory & Latency Audits. (AGENT : QA / Automation Engineer)
1- Subtask: Run Android Studio Memory Profiler on physical 2GB RAM device (`R52MC0B6KDT`). (AGENT : QA / Automation Engineer)
2- Subtask: Verify `CanvasStrokeRenderer` allocations inside `onDraw` are exactly zero. (AGENT : QA / Automation Engineer)
3- Subtask: Verify `LRU Cache` successfully evicts `StrokeChunkDto` objects when panning across 100 chunks. (AGENT : QA / Automation Engineer)

### Story: Play Store Release
1- Task: Prepare Release Build. (AGENT : DevOps Engineer)
1- Subtask: Configure `signingConfigs` in `build.gradle.kts` with release keystore. (AGENT : DevOps Engineer)
2- Subtask: Enable R8/ProGuard by setting `isMinifyEnabled = true` and `isShrinkResources = true`. (AGENT : DevOps Engineer)
3- Subtask: Write `proguard-rules.pro` to keep Protobuf serialization classes and Jetpack Ink JNI methods. (AGENT : DevOps Engineer)
2- Task: Finalize Telemetry. (AGENT : Software Engineer)
1- Subtask: Integrate `com.google.firebase:firebase-crashlytics`. (AGENT : Software Engineer)
2- Subtask: Add `FirebaseCrashlytics.getInstance().setCustomKey("canvas_type", canvasType)` before fatal exceptions. (AGENT : Software Engineer)0
# Sprint 11
## Epic 11: Post-Launch Operations & V1.1 Foundation
### Story: Production Monitoring & Hotfixes
1- Task: Monitor Day-1 Crashlytics and ANR (Application Not Responding) rates. (AGENT : DevOps Engineer)
1- Subtask: Setup Firebase Alerts for any OOM (Out of Memory) spikes exceeding the 0.1% threshold. (AGENT : DevOps Engineer)
2- Subtask: Monitor `canvas_load_time` metrics in Firebase Performance to ensure it remains under 1.5 seconds on production devices. (AGENT : DevOps Engineer)
3- Subtask: Create a hotfix branch (`hotfix/v1.0.1`) if critical bugs are identified in the Jetpack Ink rendering loop. (AGENT : Tech Lead)
2- Task: Triage user feedback and App Store reviews. (AGENT : Product Manager)
1- Subtask: Aggregate user feedback specifically regarding the ML Kit handwriting beautification accuracy. (AGENT : Product Manager)
2- Subtask: Create Jira/Linear tickets for UX friction points identified in the Dual Floating Toolbars. (AGENT : Product Manager)

### Story: V1.1 Word & PPTX Import Architecture (Deferred from V1)
1- Task: Research and prototype Apache POI memory constraints. (AGENT : Tech Lead)
1- Subtask: Create an isolated spike branch (`feature/spike-apache-poi`) to test loading a 50-slide PPTX file. (AGENT : Software Engineer)
2- Subtask: Profile the heap allocation during the PPTX parsing process to identify memory leaks. (AGENT : Software Engineer)
2- Task: Design Background Worker for Document Parsing. (AGENT : Software Engineer)
1- Subtask: Create `DocumentParserWorker.kt` extending Android `CoroutineWorker`. (AGENT : Software Engineer)
2- Subtask: Implement logic to parse `.docx` and `.pptx` files chunk-by-chunk on `Dispatchers.IO` to prevent UI thread blocking. (AGENT : Software Engineer)
3- Subtask: Write logic to convert parsed document pages into static `Bitmap` images or `TextBlockDto` objects to be injected into the `.auranote` zip file. (AGENT : Software Engineer)
3- Task: Write Integration Tests for Document Worker. (AGENT : QA / Automation Engineer)
1- Subtask: Create `DocumentParserWorkerTest.kt` class. (AGENT : QA / Automation Engineer)
2- Subtask: Write `@Test fun testWorkerSucceedsOnLargeDocument()` using `WorkManagerTestInitHelper` to verify the worker completes without exceeding the 150MB heap limit. (AGENT : QA / Automation Engineer)

***

